package com.intertec.paperanalyzer.factories;

import com.intertec.paperanalyzer.builders.ScenarioBuilder;
import com.intertec.paperanalyzer.business.PanamaPaper;
import org.testng.annotations.Test;

import java.io.InputStream;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;
import static org.testng.Assert.*;

public class PanamaPaperFactoryTest {
    @Test
    public void testGeneratePanamaPaper() throws Exception {
       /* ScenarioBuilder scenarioBuilder = mock(ScenarioBuilder.class);
        when(scenarioBuilder.buildEdged(null)).thenReturn(null);
        when(scenarioBuilder.buildEntities(null)).thenReturn(null);
        when(scenarioBuilder.buildIntermediaries(null)).thenReturn(null);
        when(scenarioBuilder.buildOfficers(null)).thenReturn(null);

        PanamaPaper panamaPaper = new PanamaPaperFactory().generatePanamaPaper(new ScenarioBuilder()); */
    }

}